image: node:latest

cache:
  paths:
    - node_modules/

heroku_deploy:
  stage: deploy
  script:
    - gem install dpl
    - dpl --provider=heroku --app=$HEROKU_APP_NAME --api-key=$HEROKU_PRODUCTION_KEY
  environment: production
